apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.prod.yml --replicas=2
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: todo-search-service
  name: todo-search-service
spec:
  replicas: 2
  selector:
    matchLabels:
      io.kompose.service: todo-search-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -f docker-compose.prod.yml --replicas=2
        kompose.version: 1.37.0 (HEAD)
      labels:
        io.kompose.service: todo-search-service
    spec:
      containers:
        - args:
            - sh
            - /home/node/app/start.sh
          env:
            - name: ELASTICSEARCH_HOST
              value: todo-elastic
            - name: ELASTICSEARCH_PORT
              value: "9200"
          image: obark1/todo-search-service:1.0
          name: todo-search-service
          ports:
            - containerPort: 3000
              protocol: TCP
          resources:
            limits:
              memory: "134217728"
      restartPolicy: Always
